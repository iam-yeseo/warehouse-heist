{
  "name": "warehouse-heist",
  "version": "1.8.3",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check dist/game.js && node --check src/index.mjs && node tests/reward-code.cjs && WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "test:reward": "node tests/reward-code.cjs",
    "dev": "wrangler dev",
    "deploy:workers": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy dist --project-name warehouse-heist"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
